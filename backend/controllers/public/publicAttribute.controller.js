import asyncHandler from "express-async-handler";

// Tệp này hiện không có hàm nào.
// Bạn có thể thêm các hàm public liên quan đến attribute ở đây.

// export const publicAttributeController = {
//
// };
